export interface ProductInterface {
    id: number
    title?: string
    description?: string
    max_price?: string
    min_price?: string
    count?: number
    orders_count?: number
    favorite_count?: number
    current_price?: number
}
